import { DirectiveDirective } from './revealon-scroll.directive';

describe('DirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
